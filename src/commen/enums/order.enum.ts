export enum OrderStatusNameEnum {
    PENDING = "PENDING",
    CONFIRMED = "CONFIRMED",
    SHIPPED = "SHIPPED",
    DELIVERED = "DELIVERED",
    CANCELLED = "CANCELLED",
    FAILED = "FAILED",
}
export enum OrderStatusEnum {
    PENDING = 0,
    CONFIRMED = 1,
    SHIPPED = 2,
    DELIVERED = 3,
    CANCELLED = 4,
    FAILED = 5,
}

export enum PaymentMethodEnum {
    CASH = "CASH",
    CARD = "CARD",
    WALLET = "WALLET",
}